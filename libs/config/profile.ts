export enum ETheme {
  Light = 'light',
  Dark = 'dark',
}

export enum EProfileRoles {
  Admin = 'admin',
  User = 'user',
  None = 'none',
}

export enum EProfileStatus {
  Active = 'active',
  Blocked = 'blocked',
}
